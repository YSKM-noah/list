# it knows you better 1.3.1

本版本打磨知识库 Agent 工作流，重点解决生成结果容易丢失的问题。

## 新增

- 知识库 Agent 输出新增最近记录。
- 最近记录支持恢复到当前预览结果。
- 最近记录支持单条删除和一键清空。
- 最近记录保存到本地 settings，最多保留 12 条。

## 说明

- 恢复历史记录只恢复到预览结果，不会自动写入知识库或 TODO，仍保持用户确认后保存的工作流。
- 本公开仓库只用于下载页和 Release 说明。GitHub 自动生成的 `Source code` 压缩包不是应用源码。
